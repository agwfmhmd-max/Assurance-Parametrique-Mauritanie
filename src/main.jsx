import React from "react";
import ReactDOM from "react-dom/client";
import ParametricInsurancePlatform from "./ParametricInsurancePlatform.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ParametricInsurancePlatform />
  </React.StrictMode>
);
